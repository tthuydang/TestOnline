<div class="list-group">
  <a href="" class="list-group-item list-group-item-action list-group-item-primary"><b>Categories</b></a>
  <% @categories.each do |item| %>
    <%= link_to("#{item.title}", root_path(:subject => item.title), class: "list-group-item list-group-item-action list-group-item-info") %>
  <% end %>
</div>
